# Fase 38 de 53 · Ports, casos de uso, payoff

**As ports de repositório, declaradas no domínio**

O enunciado desta fase está em `.docs/fase38.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 38.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase39                  # a próxima fase
git diff fase38 fase39 -- src tests  # só o que ela muda
git checkout fase37                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
