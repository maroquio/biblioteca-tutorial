# Fase 37 de 53 · O domínio

**Livro.catalogar — a entidade nasce válida**

O enunciado desta fase está em `.docs/fase37.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 37.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase38                  # a próxima fase
git diff fase37 fase38 -- src tests  # só o que ela muda
git checkout fase36                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
