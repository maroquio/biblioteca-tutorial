# Fase 19 de 53 · A API ingênua

**RF11 — o contrato de resposta**

O enunciado desta fase está em `.docs/fase19.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 19.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase20                  # a próxima fase
git diff fase19 fase20 -- src tests  # só o que ela muda
git checkout fase18                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
