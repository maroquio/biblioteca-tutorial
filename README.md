# Fase 40 de 53 · Ports, casos de uso, payoff

**O caso de uso CadastrarLivro**

O enunciado desta fase está em `.docs/fase40.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 40.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase41                  # a próxima fase
git diff fase40 fase41 -- src tests  # só o que ela muda
git checkout fase39                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
