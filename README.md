# Fase 08 de 53 · Preparação

**Roteador manual com captura de parâmetros**

O enunciado desta fase está em `.docs/fase08.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 08.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase09                  # a próxima fase
git diff fase08 fase09 -- src tests  # só o que ela muda
git checkout fase07                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
