# Fase 47 de 53 · Fatias e módulos

**Os módulos emergem do agrupamento das fatias**

O enunciado desta fase está em `.docs/fase47.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 47.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase48                  # a próxima fase
git diff fase47 fase48 -- src tests  # só o que ela muda
git checkout fase46                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
