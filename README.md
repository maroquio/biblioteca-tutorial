# Fase 20 de 53 · A dor

**⏱ TEMPO 1 — o primeiro teste automatizado da regra**

O enunciado desta fase está em `.docs/fase20.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 20.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase21                  # a próxima fase
git diff fase20 fase21 -- src tests  # só o que ela muda
git checkout fase19                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
