# Fase 51 de 53 · Fatias e módulos

**Evento de domínio e barramento in-process**

O enunciado desta fase está em `.docs/fase51.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 51.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase52                  # a próxima fase
git diff fase51 fase52 -- src tests  # só o que ela muda
git checkout fase50                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
