# Fase 05 de 53 · Preparação

**A base de autores entra por um seed**

O enunciado desta fase está em `.docs/fase05.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 05.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase06                  # a próxima fase
git diff fase05 fase06 -- src tests  # só o que ela muda
git checkout fase04                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
