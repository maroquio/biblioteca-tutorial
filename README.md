# Fase 09 de 53 · A API ingênua

**POST /livros sem nenhuma regra**

O enunciado desta fase está em `.docs/fase09.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 09.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase10                  # a próxima fase
git diff fase09 fase10 -- src tests  # só o que ela muda
git checkout fase08                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
