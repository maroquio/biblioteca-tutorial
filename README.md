# Fase 18 de 53 · A API ingênua

**RF10 — o ISBN é normalizado antes de gravar e de comparar**

O enunciado desta fase está em `.docs/fase18.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 18.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase19                  # a próxima fase
git diff fase18 fase19 -- src tests  # só o que ela muda
git checkout fase17                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
