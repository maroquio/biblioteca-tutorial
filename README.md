# Fase 24 de 53 · A primeira reação

**A reação ingênua: tirar as rotas do index.ts**

O enunciado desta fase está em `.docs/fase24.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 24.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase25                  # a próxima fase
git diff fase24 fase25 -- src tests  # só o que ela muda
git checkout fase23                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
