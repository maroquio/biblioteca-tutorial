# Fase 21 de 53 · A dor

**💣 GOLPE 1 — RF12: importação de acervo via linha de comando**

O enunciado desta fase está em `.docs/fase21.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 21.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase22                  # a próxima fase
git diff fase21 fase22 -- src tests  # só o que ela muda
git checkout fase20                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
