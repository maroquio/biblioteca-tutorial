# Fase 53 de 53 · Fatias e módulos

**Retrospectiva**

O enunciado desta fase está em `.docs/fase53.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 53.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase52  # a fase anterior
git checkout main    # o tutorial completo das 53 fases
```
