# Fase 29 de 53 · A primeira reação

**Repositório de autor — o SQL sai do serviço**

O enunciado desta fase está em `.docs/fase29.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 29.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase30                  # a próxima fase
git diff fase29 fase30 -- src tests  # só o que ela muda
git checkout fase28                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
