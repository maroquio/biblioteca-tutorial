# Fase 11 de 53 · A API ingênua

**a busca ganha título e autor**

O enunciado desta fase está em `.docs/fase11.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 11.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase12                  # a próxima fase
git diff fase11 fase12 -- src tests  # só o que ela muda
git checkout fase10                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
