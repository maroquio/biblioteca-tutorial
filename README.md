# Fase 44 de 53 · Ports, casos de uso, payoff

**As quatro camadas**

O enunciado desta fase está em `.docs/fase44.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 44.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase45                  # a próxima fase
git diff fase44 fase45 -- src tests  # só o que ela muda
git checkout fase43                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
