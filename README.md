# Fase 07 de 53 · Preparação

**Servidor HTTP com Bun.serve**

O enunciado desta fase está em `.docs/fase07.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 07.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase08                  # a próxima fase
git diff fase07 fase08 -- src tests  # só o que ela muda
git checkout fase06                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
