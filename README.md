# Fase 36 de 53 · O domínio

**A política de limite de livros**

O enunciado desta fase está em `.docs/fase36.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 36.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase37                  # a próxima fase
git diff fase36 fase37 -- src tests  # só o que ela muda
git checkout fase35                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
