# Fase 39 de 53 · Ports, casos de uso, payoff

**Os adapters SQLite implementam as ports**

O enunciado desta fase está em `.docs/fase39.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 39.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase40                  # a próxima fase
git diff fase39 fase40 -- src tests  # só o que ela muda
git checkout fase38                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
