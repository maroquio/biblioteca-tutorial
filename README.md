# Fase 50 de 53 · Fatias e módulos

**Cada módulo dono das suas tabelas — e o fim da chave estrangeira cruzada**

O enunciado desta fase está em `.docs/fase50.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 50.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase51                  # a próxima fase
git diff fase50 fase51 -- src tests  # só o que ela muda
git checkout fase49                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
