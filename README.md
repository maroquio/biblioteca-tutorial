# Fase 22 de 53 · A dor

**💣 GOLPE 2 — o limite muda, e só metade do sistema fica sabendo**

O enunciado desta fase está em `.docs/fase22.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 22.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase23                  # a próxima fase
git diff fase22 fase23 -- src tests  # só o que ela muda
git checkout fase21                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
