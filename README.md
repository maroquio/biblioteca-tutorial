# Fase 23 de 53 · A dor

**🔬 TEMPO 2 — a regra não é testável sozinha**

O enunciado desta fase está em `.docs/fase23.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 23.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase24                  # a próxima fase
git diff fase23 fase24 -- src tests  # só o que ela muda
git checkout fase22                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
