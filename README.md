# Fase 17 de 53 · A API ingênua

**RF09 — o mesmo autor, o mesmo título, duas vezes**

O enunciado desta fase está em `.docs/fase17.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 17.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase18                  # a próxima fase
git diff fase17 fase18 -- src tests  # só o que ela muda
git checkout fase16                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
