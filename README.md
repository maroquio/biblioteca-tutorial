# Fase 28 de 53 · A primeira reação

**Erros de aplicação e a tradução para HTTP num único lugar**

O enunciado desta fase está em `.docs/fase28.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 28.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase29                  # a próxima fase
git diff fase28 fase29 -- src tests  # só o que ela muda
git checkout fase27                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
