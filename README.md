# Fase 52 de 53 · Fatias e módulos

**A contagem de livros volta — agora como projeção**

O enunciado desta fase está em `.docs/fase52.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 52.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase53                  # a próxima fase
git diff fase52 fase53 -- src tests  # só o que ela muda
git checkout fase51                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
