# Fase 27 de 53 · A primeira reação

**Validação de entrada num único lugar**

O enunciado desta fase está em `.docs/fase27.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 27.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase28                  # a próxima fase
git diff fase27 fase28 -- src tests  # só o que ela muda
git checkout fase26                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
