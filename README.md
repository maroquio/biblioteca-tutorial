# Fase 10 de 53 · A API ingênua

**GET /livros/:q — busca por ISBN**

O enunciado desta fase está em `.docs/fase10.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 10.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase11                  # a próxima fase
git diff fase10 fase11 -- src tests  # só o que ela muda
git checkout fase09                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
