# Fase 12 de 53 · A API ingênua

**RF04 — autor fora da base devolve 404**

O enunciado desta fase está em `.docs/fase12.html`: objetivo, código completo
com as linhas novas destacadas, como testar e a mensagem do commit. O GitHub não
renderiza HTML — clone o repositório e abra o arquivo no navegador.

Este branch já contém o código **concluído** até a fase 12.

## Rodar

```bash
bun install
```

## Navegar

```bash
git checkout fase13                  # a próxima fase
git diff fase12 fase13 -- src tests  # só o que ela muda
git checkout fase11                  # a fase anterior
git checkout main                    # o tutorial completo das 53 fases
```
